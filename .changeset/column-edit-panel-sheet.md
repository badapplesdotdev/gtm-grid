---
"@gtmgrid/desktop": patch
---

Make the column edit panel keyboard-accessible: convert it from an inline side rail to a shadcn Sheet, so it closes on Escape and traps focus while open (matching the cell-details drawer).
