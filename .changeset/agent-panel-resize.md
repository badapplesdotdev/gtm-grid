---
"@gtmgrid/desktop": minor
---

Add drag-to-resize to the agent panel, matching the app sidebar. Drag the panel's left edge to set its width (clamped 320–720px); the width persists across launches and keeps the plan drawer aligned.
