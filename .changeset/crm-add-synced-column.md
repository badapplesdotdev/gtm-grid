---
"@gtmgrid/desktop": minor
---

Add more CRM fields to a synced table after creation: the add-column menu now has a "From HubSpot" / "From Attio" section listing the source's unmapped fields — one click adds the field as a new read-only synced column and backfills every row on the next sync.
